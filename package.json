{
  "name": "smart-model-router-gateway",
  "version": "1.1.0",
  "description": "LLM routing gateway with explicit fallback, provider abstraction and deterministic tests.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "check": "node --check src/index.ts && node --check src/config.ts && node --check src/types.ts && node --check src/modelRouter.ts && node --check src/openrouterService.ts && node --check src/server.ts && node --check tests/router.e2e.test.ts",
    "test": "node --test tests/router.e2e.test.ts"
  },
  "keywords": [
    "llm",
    "openrouter",
    "routing",
    "fastify",
    "typescript"
  ],
  "author": "UNIPDS / Erick Wendel course base; independent extensions by Gabriel Felix",
  "license": "UNLICENSED",
  "dependencies": {
    "@openrouter/sdk": "^0.5.1",
    "@types/node": "^24.13.3",
    "dotenv": "^16.6.1",
    "fastify": "^5.7.4"
  },
  "private": true
}
